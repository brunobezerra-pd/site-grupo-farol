# Environment
.env.local
.env.*.local

# Dependencies
node_modules/

# Planning documents (internal — never commit)
PRD.md
SPEC.md
ISSUES.md
KICKOFF.md
CLAUDE.md

# Internal tracking and SQL
.agents/
.claude/
skills-lock.json
sql/
# Tailwind build artifacts (regenerated by build:css)
# output.css is committed so the site works without a build step on first deploy
# Uncomment the line below if you prefer to keep it out of git
# assets/css/output.css

# OS artifacts
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
